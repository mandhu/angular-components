import { NgxPopoverDirective } from './ngx-popover.directive';

describe('NgxPopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxPopoverDirective();
    expect(directive).toBeTruthy();
  });
});
